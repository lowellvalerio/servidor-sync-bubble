services:
  - type: web
    name: sync-servidor
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
      - key: FIREBASE_STORAGE_BUCKET
        value: reportes-intenligentes.appspot.com
